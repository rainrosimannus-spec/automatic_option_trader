# Python junk
__pycache__/
*.pyc
*.pyo
.env
.env.*
venv/
.venv/
env/

# macOS junk
.DS_Store

# Config files with secrets
config/settings.yaml
config.json
credentials.json
*.key
*.pem

# Database
data/trades.db

# Logs
*.log
logs/
build/
config/settings.yaml

# Machine-generated artifacts (rewritten by running app, not source)
# Tracked once historically, untracked from 2026-05-11 onward.
config/options_universe.yaml
config/screened_universe.yaml
config/structural_risks.yaml
data/portfolio_account_cache.json
data/screener_last_run.json

# Runtime API cache (FMP fundamentals responses, refreshes monthly)
data/fmp_cache.json

# Manual backup files (rm-ed manually when no longer needed)
*.bak
*.bak-*

# Bruno (loan portfolio system)
data/bruno.db
data/bruno.db-journal

# MarsWalk (resilience backtester) — isolated DB
data/marswalk.db
data/marswalk.db-journal
data/statements/
data/backups/
data/contracts/

# Bookkeeping bridge — live-mode idempotency ledger (posted journal refs)
data/bookkeeping_posted.jsonl

# Historical backup files (kept locally, never committed)
*.v1-backup
*.pre-*
